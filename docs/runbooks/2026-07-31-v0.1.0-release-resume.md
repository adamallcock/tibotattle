---
title: v0.1.0 Release Resume Checklist
date: 2026-07-31
type: runbook
status: paused-on-branding
---

# v0.1.0 release — paused on branding, everything else ready

Verified on the development Mac, 2026-07-31. All work below is committed on
`main` and pushed.

## Done and verified

- Root suite 1491/1491; aggregate `product:check` stages green on this host;
  local-review runtime gate passed (byte-identical builds, 12/12 smokes, zero
  network attempts); R7 evidence regenerated for Node 24.14.0 + 26.2.0;
  gitleaks history scan clean; MIT LICENSE added; README is a quickstart.
- **Production service live:** <https://app-usagemonitor.adamallcock.workers.dev>
  (Worker env `production`, open enrollment with grant-backed eligibility,
  hourly aggregate cron). Envelope keys: Worker secrets + owner-only
  `apps/worker/.dev.vars.production` — back that file up privately.
- **Apple signing ready:** Developer ID Application: ADAM ALLCOCK (43RTH622SB)
  in Keychain; notary profile `usage-monitor-notary` stored and verified.
- **Sparkle updater ready:** Ed25519 keypair in login Keychain (back up the
  "Private key for signing Sparkle updates" item); public key
  `jhgPwmvWLMr7TGURJUoi6sXias7YP1F+hejZawKVTGw=`; appcast target
  `https://github.com/adamallcock/app-usagemonitor/releases/latest/download/appcast.xml`.

## Blocked on the owner's branding decision

Naming research (recommends **Quota Trace**):
`docs/research/2026-07-29-provider-neutral-product-naming-research.md`.
Icon variants were provided in the 2026-07-31 session. Once decided:

1. Rename (if chosen): update `config/product-brand.js`, UI copy in
   `apps/web/public`, release-site copy, affected tests; regenerate telemetry
   browser mirror; rerun `npm test` and `npm run product:check`.
2. Approve icon: place `AppIcon.icns` + `AppIcon.provenance.txt` (Source /
   Rights / License lines) in `apps/macos/Assets/`.
3. Production build:
   `node scripts/build-macos-app.js --output ".release-build/macos-production/<App>.app" --central-origin "https://app-usagemonitor.adamallcock.workers.dev" --external-distribution --bundle-version "0.1.0" --sparkle-framework ".release-deps/Sparkle.framework" --sparkle-appcast-url "<appcast URL above>" --sparkle-public-ed-key "<public key above>"`
4. Signed release (env: `USAGE_MONITOR_DEVELOPER_ID_APPLICATION`,
   `USAGE_MONITOR_NOTARY_PROFILE=usage-monitor-notary`,
   `USAGE_MONITOR_PRODUCTION_ORIGIN`, `USAGE_MONITOR_BUNDLE_VERSION=0.1.0`,
   `USAGE_MONITOR_SPARKLE_*`): `npm run product:macos:release`, then
   `npm run product:macos:validate:release`.
5. Tag `v0.1.0`, create the GitHub release with the DMG, `appcast.xml`
   (signed with Sparkle `sign_update`), and release notes; then flip the
   repository public — the last irreversible step, owner-confirmed.
