// Shared by the portable schema guard and both native Windows qualification
// processes. SQLite reserves every sqlite_* object name for internal use.
export const WINDOWS_SQLITE_STATE_FIXTURE_TABLE = "tibotattle_state_fixture";
