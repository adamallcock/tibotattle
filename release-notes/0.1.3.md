Fixes uploads breaking after app updates: the Keychain binding integrity check now accepts the pipeline-signed binary (verified against the Developer ID designated requirement), credential reset works even when the binding is unreadable, and a broken credential pauses sync with a visible re-approve repair path instead of silent retries.

SHA-256 `0d1a0b3d9f49437200be2d187ea8753636016f8f3d3252438c4bdbbc3ba93d77`
Signed and notarized for macOS 14.0+ (Apple Silicon). Latest installer always at https://tibotattle.com.
