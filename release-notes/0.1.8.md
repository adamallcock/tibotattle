Sign in once, stay signed in. The connect ceremony is single-flight and consumes the one-time Google proof exactly once (fixes the step-2 failure). The device credential is bound to the app's Developer ID identity so an app update never locks the app out of it, and it renews itself in the background before expiry. The dashboard session persists across relaunch, so an interrupted sign-in resumes.

SHA-256 `fba74211c64237703e644c548f3a8c14cfcce8bccb56a1e1cf9af0e066da3a3e`
Signed and notarized for macOS 14.0+ (Apple Silicon). Latest installer always at https://tibotattle.com.
