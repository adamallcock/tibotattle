## What’s new

- Adds the supported first-party Homebrew install path: `brew install --cask adamallcock/tap/tibotattle`.
- Aligns the supported and built macOS floor on macOS 14 Sonoma for Apple silicon.
- Keeps Homebrew installs on the existing signed Sparkle auto-update channel.
- Documents a narrow uninstall and zap boundary that never touches `~/.codex` and preserves TiboTattle Keychain identities.
- Removes the retired `tibotattle-client` name from source, release tooling, tests, and documentation.

The attached DMG is Developer ID signed, notarized, stapled, and Gatekeeper-verified.
