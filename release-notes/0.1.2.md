Incremental sync robustness: failures are named (never anonymous), collisions with local indexing retry in 60 seconds instead of inflating backoff, app restart re-probes a pending backlog within a minute, and a local kick route makes a stalled sync recoverable by hand.

**Install**: download the DMG below, or grab the latest from https://tibotattle.com — installed apps update automatically via Sparkle.

SHA-256 `3d536e08545cb8c75daa7923581114d1266d2c8c302e50b17e600561f5a22442`
Signed and notarized for macOS 14.0+ (Apple Silicon).
