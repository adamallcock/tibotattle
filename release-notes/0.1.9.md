Sign-in is now deterministic: the companion forwards your fresh session to the pairing request the instant enrollment returns, so the pairing no longer races the browser's cookie storage (the repeated 'contribution session not recognized' failures). Reproduced and fixed with an automated harness.

SHA-256 `5321dea1706c5c886483e1d3556c7c57211094cb8bc5bec4e7ccb466d276bcc6`
Signed and notarized for macOS 14.0+ (Apple Silicon). Latest installer always at https://tibotattle.com.
