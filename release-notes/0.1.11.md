Backfill no longer stalls mid-run, and the community allowance page is now readable point-by-point.

- **Reliable backfill:** the full-history upload could previously wedge partway through (stuck "running" with no progress). The sync controller now races each pass against a deadline so a stalled run always clears and the queue keeps draining to completion.
- **Readable allowance chart:** hover any day on the community allowance chart to read its exact figures — the central estimate, the plausible range, and how many reset fits and accounts stand behind it. Works with keyboard and touch too.
- **Sturdier community estimate:** the fitted weekly allowance now draws reliably across the whole published history, tolerating the small measurement jitter in real quota readings instead of discarding otherwise-good resets.
- **Under the hood:** cleaner plan cohorting (grouped by the provider's own plan type), longer retention so late contributions are never dropped, and hardened release/export tooling.

Sign-in from 0.1.10 carries over — no need to sign in again.

SHA-256 `d3298beb4d6cea78dc3eb25eca836e11c578fa44dd3f6ef768d2fbab59bd68cd`
Signed and notarized for macOS 14.0+ (Apple Silicon). Latest installer always at https://tibotattle.com.

Not affiliated with OpenAI or Thibault Sottiaux.
